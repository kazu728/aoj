name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Setup Deno
        uses: denolib/setup-deno@v1.1.0
        with:
          deno-version: 1.0.0
        run: deno test
        env:
          CI: true
